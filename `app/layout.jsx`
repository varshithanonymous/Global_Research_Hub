export const metadata = {
  title: "Global Research Hub",
  description: "Your one-stop hub for research insights, conferences, and academic collaboration."
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, fontFamily: "'Roboto', sans-serif", backgroundColor: "#121212", color: "#fff" }}>
        {children}
      </body>
    </html>
  );
}